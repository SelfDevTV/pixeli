const SavePixelArtForm = () => {
  //TODO: Create a safe pixel art form - save it in localstorage for now - get the id from router props and add  it to already saved canvas
  // If everything goes wrong, delete the whole canvas?

  return <p>Save</p>;
};

export default SavePixelArtForm;
